package telran.util;

public class Anagram {
	
	/**
	 *  
	 * @param word
	 * @param anagram
	 * @return true if anagrams is one of the imposible anagrams of a given word
	 * anagram is a string containing all symbols from a given word with a diferend order
	 * Exemple: yellow (wolely, lowlye, yellow), wrong anagrams (yello, yellw)
	 */
	public static boolean isAnagram(String word, String anagram) {
	 //TODO
	return false;
}
}
